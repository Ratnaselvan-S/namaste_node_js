npm -node package manager

nvm-node version manager

npx - node package executer

node repl(Read evaluate ,print,loop )

    -> node in terminal then we can run any javascript code on the terminal (behind the scene how it is running means ,javascript engine is there that is v8)

Global object in browser is window object ,like wise in a global object in node js is just "global" this gives access to setinterveal,clearout etc like these funtions
this is the super power of node js not v8 engine

like in browser if we type "this" then it will reference the object of the window but in backend that is node js "this" refers empty object

GlbalThis === Global