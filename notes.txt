 UTF8 (Unicode Transformation Format)
 -------------------------------------
    -> humans can understand the letters but computer can only understand o's and 1's so we need something to encode to understand it.
    -> Ascii is used but it is very limited and can understand only 127 character and cannot understand emojis, other symbols etc, so we need a format for globally thus
        utf8 is introduced

-> check about crypto library in node js