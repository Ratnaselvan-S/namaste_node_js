in node js every file is separate module so to connect them we will require them to our main file 

modules are protected by default it will protect variable,function and methods ,they are not exposed 
    -> the require module will run but inside if we defined variable,function etc are not accessable unless we explicilty exported

module.exports={x:x,calculateSum:calculateSum}

common Js modules                               |               Es modules(Es6 module)
 -> module.exports                              |   -> in package.json if we explicit type:"module" on the package then normal import export we do in react can be achieived in node js
 ->require()                                    |   
 ->Older way                                    |    -> Newer way
 ->Synchronous way                              |   -> Asynchronous way
 -> Non strict mode (we can define              |
 variable without  varaiable name )             |   -> strict mode (Here we need to write a variable name)
                                               


node:util this is a imp module 